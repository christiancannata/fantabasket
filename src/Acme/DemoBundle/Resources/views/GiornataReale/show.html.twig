{% extends '::base.html.twig' %}

{% block body -%}
    <h1>GiornataReale</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Votiaggiornati</th>
                <td>{{ entity.votiAggiornati }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('giornatareale') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('giornatareale_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
